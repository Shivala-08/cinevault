/**
 * CineVault Configuration Example
 * Copy this file to `config.js` and add your API key.
 * `config.js` is ignored by Git to keep your key safe.
 */

const config = {
  // 🔑 Get your key from https://www.omdbapi.com/apikey.aspx
  apiKey: 'YOUR_OMDB_API_KEY',
  apiBase: 'https://www.omdbapi.com/'
};
